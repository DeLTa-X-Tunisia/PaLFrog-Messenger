// Ce fichier est le point d'entrée du renderer process géré par Electron Forge.
// En mode production (si on n'utilise pas le serveur Vite), c'est ici que l'app React serait injectée
// ou que l'on redirigerait vers le index.html statique généré par Vite.

console.log('Electron Renderer process started');
// Si on est en prod et qu'on utilise le build Vite statique, on pourrait faire une redirection ici
// ou simplement laisser le main.js charger le bon fichier HTML.
